</li>
    <li>
      <a href="/products">HOME</a>
    </li>
    {{#if user}}
    <li><a href="/sessions/user">MI PERFIL</a></li>
    {{else}}
    <li><a href="/sessions/login">INICIAR SESIÓN</a></li>
    <li><a href="/sessions/register">REGISTRARSE</a></li>
    {{/if}}
  </ul>
</nav>
<section>
  <h2>Carrito</h2>
  <div>
    {{#each products.products}}
    {{#if this.product}}
    <div>
      <div>
        <img src={{this.product.thumbnails}} alt={{this.product.title}} />
      </div>
      <div>
        <h2>{{this.product.title}}</h2>
        <h4>${{this.product.price}}</h4>
      </div>
    </div>
    {{/if}}
    {{/each}}
  </div>
  <form action="/carts/{{cid}}/purchase" method="post">
    <button type="submit">Finalizar compra</button>
  </form>
  <form action="/carts/{{cid}}" method="post">
    <button type="submit">Vaciar carrito</button>
  </form>
</section>